# Base URL of the FastAPI backend, including the /api/v1 prefix.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1

# Which auth adapter to run against: "backend" (default) or "mock".
# "mock" restores the Sprint 17 offline adapter for UI-only work.
NEXT_PUBLIC_AUTH_ADAPTER=backend
